const arr1 = ["yes", "no", "maybe", "always", "sometimes", "never", "if"];
console.log(arr1.reverse()); // [ 'if', 'never', 'sometimes', 'always', 'maybe', 'no', 'yes' ]
console.log(arr1.sort().reverse());
